package bootcamp.pages.hotelComponents;

import bootcamp.Elements.WebComponent;
import org.openqa.selenium.WebElement;

public class HotelCard extends WebComponent {

    protected HotelCard(WebElement container) {
        super(container);
    }
}
